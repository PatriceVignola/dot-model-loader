{
  "name": "dot-model-loader-html-example",
  "version": "1.0.0",
  "description": "Pure JavaScript example for the dot-model-loader module",
  "main": "example.js",
  "scripts": {
    "install": "npm install rimraf --no-save && rimraf node_modules && npm pack ../../ && npm install *.tgz --no-save",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Patrice Vignola",
  "license": "MIT"
}
